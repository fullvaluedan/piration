{
  "name": "piration",
  "version": "1.0.0",
  "description": "Piration — offline Pirate Nation archive port for Android",
  "private": true,
  "scripts": {
    "serve": "npx --yes serve www -p 5173",
    "cap:add": "npx cap add android",
    "cap:sync": "npx cap sync android",
    "cap:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.2",
    "@capacitor/app": "^7.0.2",
    "@capacitor/core": "^7.4.2",
    "@capacitor/splash-screen": "^7.0.2",
    "@capacitor/status-bar": "^7.0.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.2"
  }
}
